#include "main.h"
/**
 *_islower - this is the entry point
 *@c: this is a variable
 *Return: this should return 0 for sucess
 */
int _islower(int c)
{


	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}

		else
		{

			return (0);
		}

	return (0);
}

