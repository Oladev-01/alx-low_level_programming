#include "main.h"
/**
 *_islower - this is the entry point
 *
 *Return: this should return 0 for sucess
 */
int _islower(int c)
{


	if (c >= 97 && c <= 122)
	{
		return (1);
	}

		else
		{

			return (0);
		}

	return (0);
}

